{
  "name": "glowstock-server",
  "version": "1.0.0",
  "description": "GlowStock backend — Express + SQLite",
  "main": "server.js",
  "scripts": {
    "start":   "node server.js",
    "dev":     "node --watch server.js",
    "migrate": "node migrate.js"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors":           "^2.8.5",
    "express":        "^4.18.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
